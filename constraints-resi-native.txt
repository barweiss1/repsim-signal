# Hard constraints for a native ReSi install.
#
# Mirrors constraints-resi-container-ngc24.06.txt's pins exactly. Those pins
# reflect real ReSi/vision-pipeline compatibility (old opencv/albumentations
# builds, pre-2.x numpy, capped transformers/datasets/huggingface_hub), not
# just an NGC base-image accident -- reuse the same known-working matrix here
# rather than letting pip drift to whatever the newest releases are.

numpy==1.24.4
opencv-python-headless==4.10.0.84
albumentations==1.3.1
pandas<2.3
pyarrow<15
transformers<4.41
datasets<2.15
huggingface_hub<0.25
protobuf<5
